---
- name: "TASK: Retrieve container info"
  community.docker.docker_container_info:
    name: "{{ immich_server_container_name }}"
  register: container_state

- name: "TASK: Set container facts"
  set_fact:
    container_exists: "{{ container_state.exists }}"
    container_image: "{{ container_state.container.Config.Image | default('tag:n/a') }}"

- name: "TASK: Set container image tag"
  set_fact:
    container_image_tag: "{{ container_image.split(':') }}"

- block:
    - include_tasks: "image_cleanup.yml"

    - name: "TASK: Pull base image"
      community.docker.docker_image:
        name: "{{ immich_server_image_base }}"
        tag: "{{ immich_image_base_tag }}"
        source: "pull"
        state: "present"
      register: "image_base_pull"

    - name: "TASK: Remove container for updates"
      community.docker.docker_container:
        name: "{{ immich_container_name }}"
        container_default_behavior: "no_defaults"
        state: "absent"
      when: container_exists and image_base_pull.changed
      with_items:
        - "{{ immich_server_container_name }}"
        - "{{ immich_microservices_container_name }}"
        - "{{ immich_machine_learning_container_name }}"
        - "{{ immich_web_container_name }}"
        - "{{ immich_proxy_container_name }}"
        - "{{ immich_typesense_container_name }}"
        - "{{ immich_redis_container_name }}"
        - "{{ immich_postgres_container_name }}"
  when: container_image_tag[1] != immich_image_base_tag

- name: "TASK: Deploy {{ immich_container_name }}"
  community.docker.docker_compose:
    project_src: "{{ immich_container_data_dir }}"
    nocache: "no"
    state: "present"
    restarted: "{{ immich_container_restart }}"
