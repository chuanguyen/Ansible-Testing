---

- name: Baseline setup
  hosts: localhost
  connection: local
  gather_facts: true
  ignore_errors: false

  roles:
    # User-related
    - clone-my-repos # Executed at the beginning as credential prompt for private repos
    # System-related
    - create-users
    - blacklist-kernel-modules
    - configure-kernel-parameters
    - install-system-packages
    - install-vagrant
    - configure-Archlinux-roles
    # Services
    - configure-systemd-services
    - configure-samba
    - configure-tlp
    - configure-ufw
    # User-related
    - install-automation-pip-packages
    - create-home-script-links
    - update-homeConfig-files
    - configure-atom
    - configure-git
    - configure-latte-dock
    # KDE-related
    - configure-konsole-yakuake
